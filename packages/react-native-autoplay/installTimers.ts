// Root re-export so `@iternio/react-native-auto-play/installTimers` is a deliberate, explicitly
// published public entry point rather than a reach into src/ internals -- see src/installTimers.ts.
import './src/installTimers';
